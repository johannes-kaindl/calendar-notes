# calendar-notes M5 — Release-Reife: i18n-Abschluss, README + Bilder, Release-Infra, Store-Vorbereitung — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Das Plugin wird veröffentlichungsfähig: vollständig zweisprachige UI (auch Kommando-Titel/Zusammenfassungen), README (de/en) mit reproduzierbar aufgenommenen Bildern, Release-Tooling nach Dach-Standard (zentrales `tools/release/`, `release.yml`, `versions.json`, CHANGELOG-Disziplin, Preflight), Lizenz/Drittlizenzen, Audit-Stand, Store-Scorecard-Vorschau `Passed` mit 0 Warnings. Was nur Jay kann (Remotes anlegen, Store-Dashboard, Rescan) landet in der Handover-Note.

**Architecture:** Keine neue Fachlogik. i18n: Deskriptoren tragen `titleKey`/`descriptionKey`, Pläne `summaryKey` + `summaryArgs`; die Obsidian-Schicht übersetzt (`t()`), die API liefert beides (Key + aufgelöster Text in aktueller Sprache). README-Bilder über Skill `readme-shots` (Rezept `scripts/shots.ts`, Fixture `fixtures/vault`, zentrale Brücke). Release über Skill `plugin-release-setup` (kopiert `tools/release-template/`, `package.json`-Delegation an `../tools/release/release.mjs`, `release.yml`).

**Spec:** §6 Release; Dach-AGENTS (Store-Flow: Developer Dashboard + Rescan; `authorUrl`; Dual-Push; Mirror-Drift), `_docs/docs/obsidian-plugin-publishing.md`, `UI-STANDARD.md`, `_docs/readme/readme-spec.json`.

## Global Constraints

- Lint 0 Warnings (= Store-Scanner); `npm run gate` + `test:integration` + `typecheck:scripts` + `smoke:gui` (beide Sektionen) grün **vor** dem Tag.
- `manifest.json`: `id: calendar-notes`, `name: "Calendar & Contact Notes"`, `description` ohne „Obsidian", `authorUrl: https://github.com/johannes-kaindl`, `minAppVersion: 1.13.0`, `isDesktopOnly: false` — Version bleibt `0.1.0` bis zum Erst-Release (dann `release.mjs`).
- Kein Push, kein Tag, kein Release in dieser Planausführung ohne explizite Freigabe — die Infra wird vorbereitet und lokal geprüft (`preflight`), der Erst-Release ist Handover.
- Branch `m5-release`; Commits deutsch mit Trailern.

---

### Task 1: i18n-Abschluss für Kommandos (Core-Keys + Übersetzung außen)

**Files:** Modify `src/core/commands/types.ts` (`titleKey`, `descriptionKey` an `CommandDescriptor` — Pflicht; `title`/`description` bleiben als englischer Fallback), `event-commands.ts`/`contact-commands.ts`/`undo`-Deskriptor (Keys `cmd.event.move.title` …), `CommandPlan.summaryKey` + `summaryArgs: (string|number)[]` (Pflicht; `summary` bleibt englischer Fallback), alle `plan()`-Implementierungen; `src/i18n/strings.ts` (alle Keys DE/EN); Obsidian-Schicht (`command-flow.ts`, `plan-preview-modal.ts`, `api.ts`: `title = t(descriptor.titleKey)`; API liefert `{ id, title, titleKey, description, descriptionKey, schema }` und Pläne mit `summary` bereits übersetzt); Tests: Registry-Test prüft, dass jeder Key in DE und EN existiert.
- [x] Commit `feat(i18n): Kommando-Titel und Plan-Zusammenfassungen zweisprachig`.

---

### Task 2: README de/en + Bilder (Skill `readme-shots`)

**Files:** Create `README.md` (en) + `README.de.md` (de) nach `_docs/readme/readme-spec.json` (Abschnitte: Was es tut · Wie es denkt (Server = Wahrheit, Spiegel, Kommandos) · Installation · Einrichtung (Konto → Discovery → Sammlungen → Profil) · Adoption · Kommandos · Plugin-API (Link `docs/API.md`) · Sicherheit/Datenschutz (Schlüsselbund, kein Mailversand, keine Telemetrie) · Grenzen (kein bidirektionaler Merge, Mobile-Hinweis Secret je Gerät) · Entwicklung (Gate, Smoke, Radicale) · Lizenz); `scripts/shots.ts` (Rezept: Settings-Tab mit Konto, Vorschau-Modal, eine Termin-Notiz im Lesemodus, Adoption-Modal, Kommando-Formular; `--focus` nötig → Hinweis an Jay: Aufnahme läuft nur, wenn Obsidian fokussiert werden darf), `docs/images/` (PNG/GIF nach Bild-Standard), `package.json` (`shots`, `shots:check`).
- [x] Aufnahme gegen den Staging-Vault fahren, sobald Fokus erlaubt ist (sonst: Platzhalter-Hinweise, README ohne Bilder committen, Bilder als Handover-Schritt) — `readme_lint.py` grün.
- [x] Commit `docs: README de/en + Bilder`.

---

### Task 3: Release-Infra (Skill `plugin-release-setup`)

**Files:** `package.json` (`release`/`version-bump`/`preflight`-Skripte mit Existenz-Guard wie die Nachbarn), `.github/workflows/release.yml` (vendored aus `tools/release-template/`), `versions.json`, `CHANGELOG.md` (Keep-a-Changelog, `[0.1.0]` vorbereitet), `LICENSE` (AGPL-3.0-or-later) + `LICENSING.md` (Dual-License-Hinweis wie vault-rag) + `THIRD-PARTY.md` (ical.js MPL-2.0, fast-xml-parser MIT — Pflicht wegen MPL), `.gitignore`-Check, `manifest.json`-Endstand, `tools/template_drift_check.py` lokal grün, `npm run preflight` grün (ohne Remotes: meldet fehlende Remotes als offen), `npm audit` dokumentiert (`docs/AUDIT.md`: 1 critical transitiv in Dev-Deps — Einordnung + Entscheidung).
- [x] Commit `chore(release): Release-Infra nach Dach-Standard vorbereitet`.

---

### Task 4: Store-Vorschau + Abschluss

**Files:** `docs/STORE.md` (Scorecard-Vorschau: Bauart-Kosten, Lint-Stand, `isDesktopOnly`, Netzwerk-Erklärung für den Review, `authorUrl`), `AGENTS.md` („Was M5 liefert" + Release-Checkliste), `docs/registry-kandidaten.md` finalisieren → Liste, was in `../REGISTRY.md` gehört, Plan-Checkboxen; Cockpit-Handover-Abschnitt „Erst-Release" (Remotes Forgejo+GitHub anlegen, `npm run release`, Developer Dashboard, Rescan).
- [x] `npm run gate && npm run test:integration && npm run typecheck:scripts`; Commit `docs: M5 abgeschlossen — releasefähig, Erst-Release als Handover`.

## Self-Review
- §6 Release vollständig (Infra, Lint=Scanner, Passed-Ziel, authorUrl, Name) → T3/T4 ✓; README-Pflicht (Store) → T2 ✓; i18n-Lücke aus M4-Review → T1 ✓; Handover-Grenzen (Account/Dashboard) benannt ✓.
