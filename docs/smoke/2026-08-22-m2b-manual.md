# M2b manueller Smoke — 2026-08-22

Beleg, dass der Obsidian-Verdrahtungs-Pfad (Task 8) einmal end-to-end gelaufen ist:
Konto anlegen → Discovery → Sammlungen aktivieren → Vorschau → Ausführen → Notizen im
Vault → zweiter Lauf ist ein No-op. Der automatisierte GUI-Smoke (CDP-Treiber,
`gui-smoke-setup`) kommt erst in M3 — das hier ist ein einmaliger Handbeleg.

## Voraussetzungen

- Build ist deployt in einen Wegwerf-Vault. In dieser Session bereits erledigt:
  ```
  OBSIDIAN_PLUGIN_DIR=<staging-vault>/.obsidian/plugins/calendar-notes npm run deploy
  ```
  Staging-Vault dieser Session:
  `/private/tmp/claude-502/-Users-Shared-code-obsidian-plugins-calendar-notes/67f95120-e208-43bc-a90f-1d8932842376/scratchpad/staging-vault/`
  — enthält bereits `.obsidian/plugins/calendar-notes/{main.js,manifest.json,styles.css}`
  und `.obsidian/community-plugins.json` mit `["calendar-notes"]` (Plugin ist vorab aktiviert).
- Radicale-Testserver mit dem getrackten Fixture (3 Termine, 2 Kontakte):
  ```
  npx tsx scripts/dav-server.ts
  ```
  (Fallback ohne `radicale` im PATH: `uvx --from radicale radicale …` — das Skript probiert
  das selbst.) Läuft auf `http://127.0.0.1:5232/`, Login `test` / `test`. `Ctrl-C` beendet.

## Schritte

1. **Vault öffnen.** Obsidian → „Open folder as vault" → den Staging-Vault-Pfad oben wählen.
2. **Plugin prüfen.** Einstellungen → Community-Plugins: „Calendar & Contact Notes" sollte
   bereits aktiv sein (per `community-plugins.json` vorbelegt); falls nicht, manuell einschalten.
3. **Konto anlegen.** Plugin-Einstellungen öffnen → „Konten" → „Konto hinzufügen":
   - Name: `Radicale Test`
   - Server-URL: `http://127.0.0.1:5232/`
   - Benutzername: `test`
   - Passwort: `test` (Secret-Feld)
4. **Verbindung testen.** Button „Verbindung testen & Sammlungen finden" — erwartet:
   Notice „2 Sammlung(en) gefunden (0 Warnung(en))" (1 Kalender + 1 Adressbuch).
5. **Sammlungen aktivieren.** Unter „Sammlungen" → Konto „Radicale Test" öffnen → für
   Kalender „kalender" und Adressbuch „kontakte" jeweils „Spiegeln" einschalten. Profile
   bleiben auf den Standard-Profilen (`default-event` / `default-contact`).
6. **Vorschau.** „Aktionen" → „Vorschau (Trockenlauf)" — erwartet: Modal mit
   „Vorschau — 5 Änderung(en)" (3 Termin-Notizen + 2 Kontakt-Notizen, alle „Neu anlegen"),
   keine Fehler, keine Hand-Edits.
7. **Ausführen.** Im Vorschau-Modal „Jetzt ausführen" — erwartet: Notizen erscheinen unter
   `Events/` (3 Stück) und `Contacts/` (2 Stück) mit befülltem Frontmatter
   (`dav_uid`, `dav_source`, `dav_etag`, Termin-/Kontaktfeldern).
8. **Zweiter Lauf.** Kommandopalette → „Alle Sammlungen synchronisieren" (oder Button
   „Jetzt synchronisieren" je Sammlung) — erwartet: keine neuen/aktualisierten Notizen
   (alle Objekte unverändert, `dav_etag` stimmt noch), Status-Zeile zeigt „0 neu, 0
   aktualisiert, 0 archiviert, 0 gelöscht, 0 Fehler."
9. **Kommandopalette stichprobenhaft.** `Cmd/Ctrl+P` → „Eine Sammlung synchronisieren…" öffnet einen
   Fuzzy-Suggester über die beiden aktivierten Sammlungen; Auswahl synchronisiert nur diese.

## Später gegen Pallas (echtes Vault)

Vor dem produktiven Einsatz **immer erst „Vorschau (Trockenlauf)"** fahren, bevor
„Jetzt ausführen" oder eine echte Sync-Intervall-Konfiguration aktiviert wird — Pallas
enthält reale Notizen, ein Fehlgriff bei Ordner-/Profilzuordnung soll dort auffallen,
bevor er schreibt. Konto mit den echten Server-Zugangsdaten anlegen, Discovery laufen
lassen, Sammlungen bewusst einzeln aktivieren (nicht alle auf einmal), pro Sammlung
zuerst die Vorschau lesen. Erst wenn die Vorschau plausibel aussieht (richtige Anzahl,
richtige Ordner, keine unerwarteten Löschungen), „Jetzt ausführen" antippen.

## Ergebnis

- [x] **2026-08-22 22:20 — per CDP gegen das laufende Obsidian 1.13.7 durchgeführt** (Staging-Vault `~/StagingVaults/calendar-notes`, Radicale 3.7.8 auf `127.0.0.1:5232`; Schritte programmatisch über `app.plugins.plugins['calendar-notes']`, nicht per Klick — die Settings-UI selbst ist damit noch nicht visuell geprüft, das übernimmt der automatisierte GUI-Smoke in M3).
  - Plugin geladen, Secret im Schlüsselbund (`secretStorage.getSecret` liest zurück).
  - Discovery: Principal `http://127.0.0.1:5232/test/`, Sammlungen `Kalender` (calendar, schreibbar) + `Kontakte` (addressbook), keine Warnungen.
  - Trockenlauf: 3 `create` (Events/2026-09-01 Zahnärztin Dr. Müller, 2026-09-07 Teamrunde, 2026-12-24 Weihnachten) + 2 `create` (Contacts/Alex Aguado, Dr. Florian Brandes); nichts geschrieben.
  - Echter Lauf: 5 Notizen angelegt; Frontmatter nach Default-Profil (`type`, `dav_uid`, `dav_source`, `dav_etag`, `dav_state: live`, `title`, `start`/`end`/`all_day`/`location`/`url`/`online`/`event_status`/`categories` bzw. `email`/`phone_*`/`organization`/`job_title`/`website`/`address`/`birthday`/`categories`), Body-Block `%% dav:begin %% … %% dav:end %%` mit Beschreibung/Link bzw. Notiz/Kanälen/Adresse/Foto-Hinweis.
  - Zweiter Lauf: beide Sammlungen `ok`, alle Zähler 0 (Kalender `etag-diff`, Adressbuch `sync-collection`).
  - State: `.obsidian/plugins/calendar-notes/state/acc1__col-calendar.json` + `acc1__col-addressbook.json`.
- [x] **22:35 — Update-/Lösch-Pfad (nach der Fix-Welle des Abschluss-Reviews):** Server-seitig `simple-1` per `PUT`+`If-Match` (Zeit + Beschreibung, `categories` entfernt) und `allday-1` + `rec-1` per `DELETE` geändert → Lauf: `update Events/2026-09-01 …` (Frontmatter `start/end/dav_etag` neu, `categories` entfernt, Body-Block neu, **keine** falschen `handEdited`), gelöschte Termine → `delete:trash` (Datei aus dem Vault verschwunden, Papierkorb-Einstellung geehrt). Vor der Korrektur in `applyDelta` wurde eine Server-Löschung bei Kalendern mit Zeitfenster fälschlich als `archive` behandelt — jetzt klassifiziert `applyDelta` anhand des gespeicherten Objekts.
- [ ] offen: visuelle Prüfung des Settings-Tabs (Konten-Unterseite, SecretComponent, Discovery-Button) und des Vorschau-Modals → M3 (`gui-smoke-setup`, Prüfpunkte aus diesem Dokument).
- Befund beim Vault-Öffnen: `open -a Obsidian <dir>` und `obsidian://open?path=` öffneten den neuen Vault nicht; `window.electron.ipcRenderer.sendSync("vault-open", path, false)` aus einem bestehenden Fenster tat es — das Fenster brauchte danach ~2 Minuten bis `readyState === "complete"`.
